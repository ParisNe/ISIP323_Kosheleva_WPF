<Page x:Class="ISIP323_Kosheleva_WPF.Pages.Step3"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Step3" Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Цены -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="Стоимость:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <StackPanel x:Name="PricePanel" Margin="0,0,0,10"/>

                <TextBlock Text="Итого:" FontSize="16" FontWeight="Bold"/>
                <TextBlock x:Name="TotalText" Text="0 ₽" FontSize="18" FontWeight="Bold" Foreground="Green"/>
            </StackPanel>

            <!-- Вывод всего чо выбрали -->
            <StackPanel Grid.Column="1">
                <TextBlock Text="Ваш выбор:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <StackPanel x:Name="ConfigPanel"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="NextButton" Content="Дальш"  Width="200" Height="45" Margin="5" Background="Green" Foreground="White"/>
        </StackPanel>
    </Grid>
</Page>