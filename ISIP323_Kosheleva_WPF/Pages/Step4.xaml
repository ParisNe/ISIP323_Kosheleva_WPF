<Page x:Class="ISIP323_Kosheleva_WPF.Pages.Step4"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Step4" Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Основной контент -->
        <Grid Grid.Row="0" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Настройки -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="Настройки кредита:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

                <TextBlock Text="Стоимость авто:"/>
                <TextBlock x:Name="CarPriceText" FontWeight="Bold" Foreground="Green" Margin="0,0,0,10"/>

                <TextBlock Text="Первоначальный взнос:"/>
                <Slider x:Name="DownSlider" Minimum="10" Maximum="90" Value="30" Margin="0,5"/>
                <TextBlock x:Name="DownPercentText" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="Срок кредита (месяцев):"/>
                <Slider x:Name="TermSlider" Minimum="12" Maximum="96" Value="36" Margin="0,5"/>
                <TextBlock x:Name="TermText" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Расчет -->
            <StackPanel Grid.Column="1">
                <TextBlock Text="Расчет платежа:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

                <TextBlock Text="Первоначальный взнос:"/>
                <TextBlock x:Name="DownAmountText" FontWeight="Bold" Margin="0,0,0,10"/>

                <TextBlock Text="Сумма кредита:"/>
                <TextBlock x:Name="LoanAmountText" FontWeight="Bold" Margin="0,0,0,10"/>

                <TextBlock Text="Ежемесячный платеж:"/>
                <TextBlock x:Name="MonthlyText" FontSize="18" FontWeight="Bold" Foreground="Red" Margin="0,0,0,10"/>

                <TextBlock Text="Общая переплата:"/>
                <TextBlock x:Name="OverpaymentText" FontWeight="Bold"/>
            </StackPanel>
        </Grid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="NextButton" Content="Дальш"  Width="80" Height="30" Margin="5" Background="Green" Foreground="White"/>
        </StackPanel>
    </Grid>
</Page>